# Munin master configuration
dbdir /var/lib/munin
htmldir /var/www/munin
logdir /var/log/munin
rundir /run/munin

# Skip reverse DNS lookups (faster in Docker)
host_name localhost

# Backend container as a node
[energygrid-backend]
    address energygrid-backend
    port 4949
    use_node_name no
